<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
      xmlns:near="clr-namespace:Strawberry.MobileApp.Pages.Near"
      x:Class="Strawberry.MobileApp.Pages.Near.NearPagePartnerView"
      VerticalOptions="Start" HorizontalOptions="Start" WidthRequest="80" HeightRequest="80"
      x:DataType="near:NearPagePartnerViewData">
    <Grid.GestureRecognizers>
        <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped"/>
    </Grid.GestureRecognizers>
    <Frame BackgroundColor="#FF0000" CornerRadius="50">
        <Frame BackgroundColor="#AAAAAA" Margin="2" CornerRadius="48">
            <Image Source="{Binding ProfileImageSource}" Aspect="AspectFill"/>
        </Frame>
    </Frame>
    <Frame VerticalOptions="Center" HorizontalOptions="Center" Padding="10,3" CornerRadius="5" TranslationY="46" BackgroundColor="#4A9CFF">
        <Label Text="{Binding Range, StringFormat='{0:#0.0}km'}" TextColor="#FFFFFF"/>
    </Frame>
</Grid>